"""App configuration."""

from django.apps import AppConfig


class DjangoSegmentsConfig(AppConfig):
    """App configuration for django-segments."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "django_segments"
